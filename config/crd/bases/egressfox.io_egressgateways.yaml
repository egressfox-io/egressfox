---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.22.0
  name: egressgateways.egressfox.io
spec:
  group: egressfox.io
  names:
    kind: EgressGateway
    listKind: EgressGatewayList
    plural: egressgateways
    singular: egressgateway
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.engine
      name: Engine
      type: string
    - jsonPath: .status.conditions[?(@.type=="Published")].status
      name: Published
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          EgressGateway requests validated engine configuration publication and may
          explicitly opt into one operator-managed engine workload.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: EgressGatewaySpec binds one pool to a pinned engine renderer
              and owned output Secret.
            properties:
              engine:
                enum:
                - Mihomo
                - SingBox
                type: string
              listener:
                description: Listener is BYO-only. Managed mode owns a fixed authenticated
                  Pod listener.
                properties:
                  address:
                    default: 127.0.0.1
                    description: M3 accepts loopback listeners only.
                    enum:
                    - 127.0.0.1
                    - ::1
                    type: string
                  port:
                    default: 1080
                    format: int32
                    maximum: 65535
                    minimum: 1
                    type: integer
                type: object
              outputSecretName:
                maxLength: 253
                minLength: 1
                type: string
              poolRef:
                properties:
                  name:
                    maxLength: 253
                    minLength: 1
                    type: string
                required:
                - name
                type: object
              runtime:
                description: Runtime omitted means BYO for backward compatibility.
                properties:
                  managed:
                    description: |-
                      ManagedRuntimeSpec opts a Gateway into the release-controlled M7 runtime.
                      M7 intentionally exposes no Pod template, image, replica or Service knobs.
                    type: object
                type: object
                x-kubernetes-validations:
                - message: runtime must select managed mode
                  rule: has(self.managed)
            required:
            - engine
            - poolRef
            type: object
            x-kubernetes-validations:
            - message: outputSecretName is required for BYO mode and forbidden for
                managed mode
              rule: 'has(self.runtime) ? !has(self.outputSecretName) : has(self.outputSecretName)'
            - message: listener is BYO-only and must be omitted for managed mode
              rule: '!has(self.runtime) || !has(self.listener)'
          status:
            description: EgressGatewayStatus never reports credentials, endpoint IDs
              or artifact digests.
            properties:
              activeGeneration:
                type: string
              clientAuthSecretName:
                type: string
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              eligibleEndpoints:
                format: int32
                type: integer
              lastPublishedTime:
                format: date-time
                type: string
              observedGeneration:
                format: int64
                type: integer
              publishedGeneration:
                description: |-
                  PublishedGeneration and ActiveGeneration are opaque, random Kubernetes
                  object names. They are not content or credential digests.
                type: string
              selectedEndpoints:
                format: int32
                type: integer
              serviceName:
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
