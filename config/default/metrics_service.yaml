apiVersion: v1
kind: Service
metadata:
  name: egressfox-metrics
spec:
  selector: {app.kubernetes.io/name: egressfox, app.kubernetes.io/component: operator}
  ports:
    - {name: https, port: 8443, targetPort: metrics}
